const owls = ["barn", "spotted", "horned"]

const [b, s, h] = owls

console.log(b, h, s)

const greatHorned = {
    region: "North America",
    population: 330000,
    genus: "Avidae"
}

const { population } = greatHorned

console.log(population)